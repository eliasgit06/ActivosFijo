@model IEnumerable<ActivosFijo.Models.TblUsuario>

@{
    ViewBag.Title = "Index";
}

<div class="row mt-5">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-header border-0">
                <div class="row align-items-center">
                    <div class="col">
                        <h3 class="mb-0">Lista de Usuarios</h3>
                    </div>
                    <div class="col text-right">
                        <a class="nav-link active" href="@Url.Action("Create","TblUsuarios")">
                            <i class="fa fa-plus-circle text-success"></i>
                        </a>
                    </div>
                </div>
            </div>
            <div class="table-responsive">
                @*Table de informacion*@
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">
                                Acción
                            </th>
                            <th scope="col">
                                @Html.DisplayNameFor(model => model.cUsuario)
                            </th>
                            <th scope="col">
                                @Html.DisplayNameFor(model => model.cClave)
                            </th>
                            <th scope="col">
                                @Html.DisplayNameFor(model => model.cNombre)
                            </th>
                            <th scope="col">
                                @Html.DisplayNameFor(model => model.rol)
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <div class="row">
                                        <a class="nav-link active" href="@Url.Action("Edit","TblUsuarios", new { id = item.Id })">
                                            <i class="fa fa-edit text-success"></i>
                                        </a>
                                        <a class="nav-link active" href="@Url.Action("Details","TblUsuarios", new { id = item.Id })">
                                            <i class="fa fa-eye text-info"></i>
                                        </a>
                                        <a class="nav-link active" href="@Url.Action("Delete","TblUsuarios", new { id = item.Id })">
                                            <i class="ni ni-fat-remove text-danger mh-100"></i>
                                        </a>
                                    </div>
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.cUsuario)
                                </td>
                                <td>
                                    @Html.Raw("\u2022\u2022\u2022")
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.cNombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.rol)
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@*<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cClave)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rol)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cClave)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>*@
